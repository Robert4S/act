actor summing {
	state sum: Num;

	initialiser {
		send(summing, 10000);
		return 0;
	}

	update (num: Pid('a)) {
		if (num <= 0) {
			intrinsic(print_value, sum);
			intrinsic(print_value, "\n");
			return sum;
		};
		send(summing, num - 1);
		return num + sum;
	}
}
